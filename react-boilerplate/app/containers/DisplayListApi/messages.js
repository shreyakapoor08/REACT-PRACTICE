/*
 * DisplayListApi Messages
 *
 * This contains all the text for the DisplayListApi container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DisplayListApi';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DisplayListApi container!',
  },
});
